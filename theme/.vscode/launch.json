{
    "version": "1.0.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9001,
            "pathMappings": {
                "/var/www/html/wp-content/themes/skeleton": "${workspaceFolder}",
            }
        },
    ]
}